DISCORD_TOKEN="YOUR_DISCORD_USER_TOKEN"
MAIN_WEBHOOK="https://discord.com/api/webhooks/WEBHOOK_ID/WEBHOOK_TOKEN"
ERROR_WEBHOOK="https://discord.com/api/webhooks/WEBHOOK_ID/WEBHOOK_TOKEN"
GITHUB_TOKEN="ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
REPOSITORY="YOUR_USERNAME/YOUR_REPO_NAME"
PING_ROLE_ID=""
LOCALE="en-US"
# Optional: proxy list for country-cycling quest fetches. JSON array, JSON
# {"proxies":[...]}  object, or a plain newline/comma-separated list of
# proxy URLs (e.g. https://user:pass@host:port). Keep this in a GitHub
# secret, never commit real values in proxy.json.
PROXY_LIST=""
